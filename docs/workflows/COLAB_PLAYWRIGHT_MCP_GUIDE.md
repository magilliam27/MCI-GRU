# Colab Playwright MCP Agent Guide (Superseded)

Last updated: 2026-06-18

This file is retained so older handoffs and links do not break.

Default automated Colab control now lives in
[`COLAB_CHROME_CONTROL_GUIDE.md`](COLAB_CHROME_CONTROL_GUIDE.md). Use
`chrome:control-chrome` for new Colab automation.

Use Playwright MCP only as a legacy fallback when Chrome control is unavailable,
blocked, or explicitly requested. Record why the fallback was used before
acting.
