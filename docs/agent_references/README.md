# Agent References

Historical, non-Codex tool guidance kept out of the repository root.

- `claude/CLAUDE.md` is the previous Claude-specific guidance. It stays until the
  ticket that owns the root `CLAUDE.md` retires it and fixes the pointer there.
- The Cursor MCP config, rules, workspace file, and plans were retired in
  2026-09 (map #211) and are readable at tag `archive/pre-cleanup-2026-09`. The
  one plan `AGENTS.md` still cites moved to
  `docs/research/archive/graph_signal_upgrades_plan_2026-04.md`.

Use `AGENTS.md` at the repository root as the entrypoint.
