# Agent References

This directory keeps non-Codex tool guidance out of the repository root while
preserving it as reference material.

- `claude/` contains the previous Claude-specific guidance and local settings.
- `cursor/` contains the previous Cursor MCP config, rules, workspace file, and plans.

Use `AGENTS.md` at the repository root as the Codex-first entrypoint.
