# @package _global_
# Ablation arm A3 (issue 164 protocol): per-node top-K=20 by signed correlation
# on the static cadence the frozen recipe pins. This is the static sibling of
# correlation_dynamic_topk20_pos.yaml, which stays as the 6-month dynamic
# preset. Depth 20 is deliberate: it re-litigates April 2026's anti-top-K
# result under the corrected protocol (PIT-admissible candidate selection, a
# populated rank-4 edge tensor), and the density child showed top-K depths are
# a nested ladder that data grounds cannot separate until one beats the
# control.

experiment_name: graph_topk20_static

graph:
  update_frequency_months: 0
  top_k: 20
  top_k_metric: corr
